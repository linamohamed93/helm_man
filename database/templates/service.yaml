apiVersion: v1
kind: Service
metadata:
  name: {{ include "database.fullname" . }}
spec:
  selector:
    app: redis
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP
